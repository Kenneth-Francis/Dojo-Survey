<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Dojo Survey Submission</title>
</head>
<body>

    <div class="container col mt-5 mx-auto w-25">
        <div class="card-body">
            <div class="card shadow">
                <div class="card-header">
                    <h1 class="text-center text-primary-emphasis">Submitted Info</h1>
                </div>
                <div class="card-body w-100 m-auto">
                    <div class="mb-3 d-flex justify-content-between">
                        <p class="fs-5 text-warning-emphasis">Name:</p>
                        <p class="w-50 fs-5">@ViewBag.Name</p>
                    </div>
                    <div class="mb-3 d-flex justify-content-between">
                        <p class="fs-5 text-warning-emphasis" >Dojo Location:</p>
                        <p class="w-50 fs-5">@ViewBag.Location</p>
                    </div>
                    <div class="mb-3 d-flex justify-content-between">
                        <p class="fs-5 text-warning-emphasis" >Favorite Language:</p>
                        <p class="w-50 fs-5">@ViewBag.Language</p>
                    </div>
                    <div class="mb-3 d-flex justify-content-between">
                        <p class="fs-5 text-warning-emphasis">Comment (optional):</p>
                        <p class="w-50 fs-5">@ViewBag.Comment</p>
                    </div>
                    <div class="mb-3 d-flex justify-content-between">
                        <a class="btn btn-success fs-5" href="/">Go Back</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
</body>
</html>